# Текущий контекст работы над sfml-dungeon-gen

## Последние изменения (Сессия: Прототип V2 и Очистка)
- **Удаление Legacy**: Все старые C++ исходники (`sources`, `build`, `bin`, `cmake_scripts`) удалены из основного проекта. Проект полностью мигрировал на стек Rust/Tauri.
- **Интеграция подтверждена**:
  - Реализован и запущен прототип на **Tauri 2.0**.
  - Настроена связь (IPC) между Rust-ядром (`src-tauri`) и React-интерфейсом (`src`).
  - Тестовые данные из Rust (на базе `nalgebra`) успешно отображаются на холсте **React Flow**.
- **Инфраструктура**:
  - Исправлены ошибки компиляции Rust (проблема дублирования макросов в `lib.rs`).
  - Добавлена иконка и настроен скрипт сборки `build.rs`.

## Текущие задачи
1. **PROC-12: Core Node System**:
   - Реализация системы кастомных нод в React Flow.
   - Создание базовой логики графа на Rust (связывание параметров нод).
2. **UI/UX**:
   - Разработка боковой панели (Sidebar) для добавления нод.
   - Интеграция "Property Inspector" для редактирования параметров Rust-объектов.

## Текущее состояние
Фундамент V2 заложен. Проект компилируется и работает. Следующий этап — превращение простого холста в полноценный нодовый редактор.
